<Window x:Class="MinecraftLauncherPerso.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Launcher du serveur" Height="320" Width="480"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Launcher du serveur" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="RAM allouée (Mo) :" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox x:Name="MaxRamTextBox" Width="80" Text="6144" />
        </StackPanel>

        <TextBox Grid.Row="2" x:Name="StatusLogTextBox" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" Background="#FF1E1E1E" Foreground="#FFDDDDDD"
                 FontFamily="Consolas" Margin="0,0,0,12" />

        <ProgressBar Grid.Row="3" x:Name="ProgressBar" Height="18" Minimum="0" Maximum="100" Margin="0,0,0,12" />

        <Button Grid.Row="4" x:Name="PlayButton" Content="Jouer" Height="36" Click="PlayButton_Click" />
    </Grid>
</Window>
